spring:
  application:
    name: openshift-sb-ms-store
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888/configserver/}
    kubernetes:
      secrets:
        enabled: true
        enableApi: true
      reload:
        enabled: true 
